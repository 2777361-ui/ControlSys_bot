# Одно приложение Amvera: веб-интерфейс + Telegram-бот, общая БД
meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  command: sh start.sh
  persistenceMount: /data
  containerPort: 80
